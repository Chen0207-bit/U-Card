# U-Card Demo 完整化实施计划

## 1. 项目目标

将当前 `ucard-demo` 从“可展示业务闭环的原型 Demo”升级为“复杂、完整、可连续演示的 U-Card 综合业务系统”。

优先保证现场 Demo 稳定，再逐步增加平台化、风控、账本、审批、企业账户、商户和 BI 能力。

项目目录：

```text
D:\Project\demos\ucard-demo
```

当前技术约束：

- 原生 HTML/CSS/JavaScript
- Node.js + HTTP Server
- Cloudflare Worker 兼容入口
- 当前使用内存数据库和种子数据
- 不要为了 Demo 引入大型前端框架
- 不要接入真实支付、真实银行卡、真实 KYC 或真实资金

## 2. 当前已有能力

已有后台页面：

- 驾驶舱
- U 卡管理
- KYC 审核
- 交易流水
- CRM 客户管理
- 业绩排行
- 佣金管理
- 分销链路
- 积分管理
- 商城管理
- 兑换订单

已有用户端：

- 卡包
- 充值
- 消费
- 交易记录
- 积分
- 签到和任务
- 积分商城
- 订单
- KYC
- 邀请推广
- PC 端和移动端 H5

核心文件：

- `core.js`：数据模型、种子数据、业务逻辑、API 路由
- `server.js`：本地 HTTP 服务
- `worker.js`：Cloudflare Worker 入口
- `public/admin.html`：运营后台
- `public/app.html`：移动端 H5
- `public/app-pc.html`：用户端 PC 页面

## 3. 执行规则

### 3.1 实施原则

1. 每完成一个阶段，必须运行服务并进行真实页面验证。
2. 不要只修改 UI；每个关键按钮都要有对应 API 或明确的 Demo 状态变更。
3. 所有业务操作都要有成功、失败、空数据、加载中和权限不足状态。
4. 所有金额统一使用 USD 展示，并保留两位小数。
5. 卡号、CVV、手机号等敏感信息必须掩码显示。
6. 所有接口都要做基本输入校验，禁止产生 `undefined`、`NaN` 或空白错误页。
7. 保留现有种子数据和演示账号，不要破坏已有演示动线。
8. 真实支付、真实链上交易、真实证件 OCR 暂时全部使用模拟适配器。

### 3.2 每阶段验收

每个阶段结束后执行：

```text
1. node --check core.js
2. node --check server.js
3. 启动本地服务
4. 检查所有 API 返回状态
5. 检查后台所有菜单
6. 检查用户端所有底部菜单
7. 检查浏览器 console 无 Error
8. 检查页面无 NaN、undefined、加载失败
```

## 4. P0：恢复并稳定主 Demo

### P0.1 修复驾驶舱 GMV

文件：

- `public/admin.html`
- `core.js`

问题：后端返回 `topup` 和 `consume`，前端使用不存在的 `gmv` 字段，造成 GMV 为 0、完成度为 `NaN%`。

要求：

```js
const gmv = row.topup + row.consume;
const completion = row.target ? gmv / row.target * 100 : 0;
```

验收：

- 销售排行榜显示真实 GMV
- 目标完成度显示正常百分比
- 总监、一级、二级、三级销售数据可见
- 无任何 `NaN`

### P0.2 统一日期格式

文件：

- `core.js`
- `public/admin.html`
- `public/app.html`
- `public/app-pc.html`

要求：

- 新增统一 `formatDate()`
- 统一显示 `YYYY-MM-DD HH:mm`
- 后台和用户端同一笔交易显示相同时间
- 种子数据使用当前时间倒推，不要出现无意义的 `01-01`

### P0.3 用户端冻结、解冻、挂失

新增 API：

```text
POST /api/app/card/freeze
POST /api/app/card/unfreeze
POST /api/app/card/lost
```

要求：

- 用户端操作后立即刷新卡状态
- 后台查看同一卡片时状态同步
- 冻结卡不能消费和充值
- 挂失卡不能恢复为 active
- 操作产生交易/审计记录

### P0.4 Demo 数据重置

新增：

```text
POST /api/demo/reset
```

后台增加“重置演示数据”按钮。

要求：

- 恢复所有种子数据
- 清空现场产生的交易、积分、订单、审批
- 重置后自动刷新当前页面
- 必须二次确认

## 5. P1：完善现场展示效果

### P1.1 驾驶舱增强

增加指标：

- GMV
- 充值总额
- 消费总额
- 活跃用户数
- 卡片数量
- 待审 KYC
- 待发货订单
- 待结算佣金
- 风险交易数
- 近 14 天趋势

增加交互：

- 点击指标进入对应模块
- 点击销售进入销售详情
- 时间范围切换：今日、本周、本月、本季度
- 区域筛选

### P1.2 客户详情全景页

客户详情必须展示：

- 基础资料
- CRM 阶段
- 所属销售
- 跟进时间轴
- 下次跟进时间
- 关联卡片
- 充值记录
- 消费记录
- 积分流水
- KYC 状态
- 佣金贡献
- 标签和备注

新增 API：

```text
GET /api/admin/customers/:id/overview
```

### P1.3 销售目标管理

增加目标配置：

- 个人目标
- 团队目标
- 区域目标
- 月度目标
- 季度目标
- 年度目标

目标字段：

- 发卡量
- 充值额
- 消费额
- 积分发放量

增加完成率、排名、趋势和成员对比。

### P1.4 分销链路增强

每笔交易展示完整佣金路径：

```text
客户消费 $100
→ 直属销售 1%
→ 上级销售 2%
→ 上上级销售 0.5%
→ 佣金合计
```

增加：

- 来源用户
- 来源卡片
- 交易类型
- 交易金额
- 每级佣金比例
- 每级佣金金额
- 结算状态
- 关联单号

### P1.5 风控模拟中心

新增后台菜单：

```text
风控中心
├── 风险事件
├── 风险规则
├── 黑名单
├── 白名单
└── 风险标签
```

模拟规则：

- 单笔交易超过阈值
- 24 小时交易次数过多
- 短时间跨国家消费
- 连续支付失败
- 新设备大额充值

功能：

- 风险等级
- 风险原因
- 自动冻结
- 人工复核
- 解除风控
- 风控事件时间轴

### P1.6 财务对账中心

新增后台菜单：

```text
财务结算
├── 充值对账
├── 消费对账
├── 退款对账
├── 差异清单
├── 商户结算
└── 财务报表
```

支持模拟字段：

- 应入账金额
- 实际入账金额
- 手续费
- 差异金额
- 对账状态
- 结算周期
- 结算凭证

至少实现 CSV 导出。

## 6. P2：完善用户端和商城

### P2.1 用户端个人中心

增加：

- 安全设置
- 修改密码 Demo
- 消息通知
- 帮助中心
- 客服入口
- 语言切换
- 退出登录确认

### P2.2 积分中心增强

增加：

- 可用积分
- 冻结积分
- 即将过期积分
- 积分有效期
- 积分来源分类
- 积分消耗分类
- 任务进度
- 任务领取状态

`/api/app/task/claim` 必须防止重复领取。

### P2.3 商城增强

增加：

- 商品详情
- 分类筛选
- 搜索
- 限购数量
- 兑换确认
- 虚拟商品核销码
- 实物商品物流
- 售后申请
- 订单取消
- 积分退回
- 库存回补
- 评价晒单

## 7. P3：补齐基础后台能力

新增系统管理菜单：

```text
系统管理
├── 账号管理
├── 角色管理
├── 组织架构
├── 权限配置
├── 系统参数
├── 字典管理
├── 登录日志
└── 操作日志
```

先做可交互 Demo，不要求接入真实数据库。

最低要求：

- 角色列表
- 角色权限树
- 账号启用/禁用
- 销售组织树
- 操作日志列表
- 登录日志列表
- 权限不足页面

## 8. P4：平台化复杂系统

目标：从单租户 Demo 升级为可扩展的多租户业务平台。

### P4.1 多租户体系

新增模型：

```text
Tenant
TenantUser
TenantConfig
TenantRole
TenantPaymentChannel
TenantCommissionRule
```

功能：

- 平台创建租户
- 租户审核
- 租户 Logo、域名、品牌色
- 租户级卡产品
- 租户级佣金规则
- 租户级积分规则
- 租户级语言、币种、时区
- 租户数据隔离

验收：

- 租户 A 看不到租户 B 的用户、卡、交易和佣金
- 不同租户可配置不同佣金比例
- 租户管理员不能访问平台管理功能

### P4.2 工作流与审批中心

新增流程：

```text
发卡申请 → 运营审核 → 发卡完成
KYC 升级 → 风控审核 → 通过/驳回
退款申请 → 财务审核 → 执行退款
佣金结算 → 财务确认 → 打款
调账申请 → 多级审批 → 执行
```

功能：

- 我的待办
- 我发起的
- 抄送我的
- 审批模板
- 审批节点
- 会签/或签
- 加签
- 驳回和撤回
- 转交
- 审批超时
- 审批记录

### P4.3 风控规则引擎

把 P1 的模拟风控升级为可配置规则：

```text
条件：交易金额 > 1000
条件：国家不在允许地区
条件：24 小时交易次数 > 20
动作：拦截 / 冻结 / 人工审核 / 标记
```

支持：

- 规则优先级
- 规则启停
- 风险评分
- 规则命中记录
- 风险策略版本
- 人工复核

### P4.4 钱包、账本和资金流水

新增模型：

```text
Wallet
WalletAccount
LedgerEntry
BalanceSnapshot
FrozenBalance
SettlementAccount
```

每笔资金动作必须写入账本。

充值示例：

```text
渠道账户 +100
用户卡账户 +99
手续费账户 +1
```

消费示例：

```text
用户卡账户 -100
商户待结算账户 +98
平台手续费账户 +2
```

要求：

- 余额可追溯
- 退款产生反向流水
- 不允许直接覆盖历史流水
- 支持冻结余额和待结算余额

### P4.5 开放 API 平台

新增菜单：

```text
开放平台
├── 应用管理
├── API Key
├── OAuth 应用
├── Webhook
├── API 日志
├── 限流策略
└── 接口文档
```

至少实现模拟 API：

- 用户开户
- KYC 提交
- 发卡
- 查询余额
- 查询交易
- 充值回调
- 消费回调
- 退款
- 积分查询
- 订单查询

### P4.6 消息通知中心

支持渠道：

- 站内信
- 邮件 Demo
- 短信 Demo
- WhatsApp Demo
- Webhook
- App Push Demo

支持模板变量：

```text
{{userName}}
{{amount}}
{{cardLast4}}
{{transactionId}}
{{createdAt}}
```

## 9. P5：生产级全球支付生态

目标：形成接近真实商业系统的全球支付、企业卡、商户和合规平台。

### P5.1 支付和发卡编排层

新增适配器：

```text
Payment Orchestrator
├── Fiat Gateway Adapter
├── Crypto Gateway Adapter
├── Card Issuer Adapter
├── KYC Adapter
├── FX Adapter
└── Settlement Adapter
```

能力：

- 多渠道路由
- 渠道优先级
- 渠道健康检查
- 自动故障切换
- 费率比较
- 幂等控制
- 异步回调
- 超时补偿
- 对账差异处理

交易状态机：

```text
created → pending → processing → success
                         ├→ failed
                         ├→ reversed
                         └→ refunded
```

### P5.2 合规、KYC、KYB、AML

新增合规中心：

```text
合规中心
├── KYC
├── 企业 KYB
├── AML 筛查
├── 制裁名单
├── PEP 名单
├── 可疑交易报告
├── 证件管理
└── 合规案件
```

功能：

- C 端 KYC
- B 端企业 KYB
- UBO 实际受益人
- 制裁名单匹配
- PEP 筛查
- 风险等级
- 可疑交易报告
- 证件有效期提醒
- 国家/地区政策限制

真实第三方接入先用 Mock Adapter。

### P5.3 企业账户和企业卡

企业角色：


```text
企业管理员
财务人员
普通员工
审批人
持卡员工
```

功能：

- 企业注册和认证
- 子账户
- 批量发卡
- 企业额度
- 部门额度
- 员工卡限额
- 消费审批
- 企业账单
- 发票
- 企业充值
- 成本中心
- 部门报表

演示流程：

```text
企业充值 → 分配部门预算 → 员工消费 → 部门审批 → 企业结算
```

### P5.4 商户收单和结算平台

新增商户端：

```text
商户平台
├── 商户入驻
├── 商户 KYC
├── 收款订单
├── 退款管理
├── 结算账户
├── 费率配置
├── 对账单
└── 经营报表
```

支持：

- 商户入驻审核
- 商户费率
- 商户订单
- 商户退款
- T+0 / T+1 / T+N 结算
- 分账
- 商户风控
- 结算凭证

### P5.5 BI 和数据中心

新增数据中心：

```text
数据中心
├── 实时指标
├── 交易分析
├── 用户分析
├── 销售分析
├── 佣金分析
├── 积分成本
├── 商户分析
├── 风控分析
└── 自定义报表
```

指标：

- DAU / MAU
- 开卡转化率
- KYC 通过率
- 首充率
- 消费率
- 留存率
- 用户 LTV
- CAC
- GMV
- 净收入
- 佣金成本
- 积分成本
- 风险交易率
- 退款率

支持时间、区域、销售、卡等级、商户等多维筛选。

### P5.6 生产运维能力

为生产化预留：

- MySQL/PostgreSQL 持久化层
- Redis 缓存
- 消息队列
- 对象存储
- 定时任务
- 分布式锁
- 审计日志
- 服务监控
- 链路追踪
- 错误告警
- 数据备份
- 灾备恢复
- 灰度发布
- Feature Flag
- API 限流
- 数据脱敏

## 10. 最终演示动线

最终 Demo 至少展示以下角色：

```text
平台超级管理员
租户管理员
运营人员
销售人员
财务人员
持卡用户
企业管理员
商户管理员
```

推荐现场流程：

```text
平台创建租户
→ 租户配置卡产品和佣金规则
→ 销售创建客户
→ 客户提交 KYC
→ 运营审核并发卡
→ 用户充值
→ 用户消费
→ 风控引擎识别交易
→ 系统生成积分和多级佣金
→ 财务完成对账
→ 商户查看结算单
→ 管理层查看 BI 看板
```

## 11. 推荐开发顺序

```text
P0：修复现有错误，保证 Demo 可运行
P1：驾驶舱、客户详情、风控、对账、分销链路
P2：用户端、积分、商城、售后
P3：系统管理、RBAC、日志、多语言
P4：多租户、审批、账本、开放 API、消息中心
P5：支付编排、合规、企业卡、商户、BI、生产运维
```

## 12. 交付要求

每个阶段完成后输出：

1. 修改文件列表
2. 新增 API 列表
3. 新增页面和菜单列表
4. 已验证的演示流程
5. 未完成项
6. 发现的风险和后续建议

不要一次性重写整个项目。优先小批量提交，每个阶段保持项目可运行。
